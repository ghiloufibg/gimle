---
sidebar_position: 2
---

# CLI reference

_Content pending._ Will mirror `gimle-cli`'s own `GimleCli.usage()` text, expanded with examples
for each verb (`get`, `apply`, `delete`, `set`, `logs`).
