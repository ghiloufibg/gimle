---
sidebar_position: 2
---

# Node topology

_Content pending._ Will cover the three Java process roles — Node Agent, Worker JVM, Control
Plane — and how node/worker/module failure are distinct events reconciled at different costs
(seconds/sub-second/milliseconds).
