---
sidebar_position: 4
---

# Deploy your first module

_Content pending._ Will be built around the real `gimle-examples/greeter-provider` and
`greeter-consumer` pair — a genuine cross-worker fabric service call, not a toy example.
