---
sidebar_position: 3
---

# Writing a module manifest

_Content pending._ Will walk through `gimle-module.yaml` field by field: name/version,
required-module version ranges, exported services, isolation tier, resource requests/limits,
health probe class, and lifecycle hooks.
