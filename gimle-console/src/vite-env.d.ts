/// <reference types="vite/client" />

// Injected by vite.config.ts's `define`, from package.json's "version" field -- the single source
// of truth for the version shown in the Overview footer and the sidebar (see F-08).
declare const __APP_VERSION__: string;
