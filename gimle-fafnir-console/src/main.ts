// Placeholder entry point -- replaced once Lovable's real Gimlé Fafnir Vault SPA lands in this
// directory (see vite.config.ts's own comment). Deliberately does nothing beyond what index.html's
// own inline placeholder already shows.
export {};
