#!/usr/bin/env bash
# Disabled: a full `mvn verify` per commit stopped being practical once the reactor grew to this
# many modules -- each commit was paying for a full uncached rebuild across the whole project, not
# just what actually changed. Re-enable once that's no longer true (e.g. wiring this up to
# `./mvnw verify`, which is enough to pick up the build-cache extension the local `mvn` on PATH
# still can't use, and would then only really rebuild what a given commit's modules depend on).
exit 0
