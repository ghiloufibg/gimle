# kind: Deployment -- three replicas so the fabric's own load balancer actually has more than one
# live mapreduce-worker to spread mapreduce-coordinator's chunks across; see ../README.md for the
# full story of how that fan-out happens.
kind: Deployment
name: mapreduce-worker-deployment
module:
  name: com.example.mapreduce.worker
  version: 1.0.0
artifactPath: gimle-examples/mapreduce-wordcount/mapreduce-worker/target/mapreduce-worker.jar
replicas: 3
