<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 http://maven.apache.org/xsd/assembly-1.1.3.xsd">
  <!--
    The standalone Hilmir archive: bin/hilmir plus exactly gimle-hilmir's own runtime dependency
    closure, nothing else; confirmed via `mvn -pl gimle-hilmir dependency:tree -Dscope=runtime`.
    gimle-mimir is part of that closure (a real, unscoped compile dependency in gimle-hilmir's own
    pom.xml) even though gimle-hilmir never spawns com.gimle.mimir.StoreMain itself: `hilmir store
    add`/`store remove` talk to an already-running store cluster directly over StoreClient, the
    same binary Raft-membership-change RPC gimle-mimir uses internally; omitting it here left
    those two commands throwing an uncaught NoClassDefFoundError from this archive.
  -->
  <id>hilmir</id>
  <formats>
    <format>tar.gz</format>
  </formats>
  <includeBaseDirectory>true</includeBaseDirectory>
  <baseDirectory>gimle-hilmir-${project.version}</baseDirectory>

  <dependencySets>
    <dependencySet>
      <outputDirectory>lib</outputDirectory>
      <useProjectArtifact>false</useProjectArtifact>
      <unpack>false</unpack>
      <scope>runtime</scope>
      <includes>
        <include>com.gimle:gimle-hilmir</include>
        <include>com.gimle:gimle-core</include>
        <include>com.gimle:gimle-mimir</include>
        <include>org.slf4j:slf4j-api</include>
        <include>ch.qos.logback:logback-classic</include>
        <include>ch.qos.logback:logback-core</include>
        <include>org.yaml:snakeyaml</include>
      </includes>
    </dependencySet>
  </dependencySets>

  <fileSets>
    <fileSet>
      <directory>${project.basedir}/src/main/dist/bin</directory>
      <outputDirectory>bin</outputDirectory>
      <fileMode>0755</fileMode>
      <includes>
        <include>hilmir</include>
        <include>hilmir.cmd</include>
      </includes>
    </fileSet>
  </fileSets>
</assembly>
